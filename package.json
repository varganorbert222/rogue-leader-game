{
  "name": "rogue-leader-game",
  "private": true,
  "workspaces": [
    "frontend",
    "engine",
    "game"
  ],
  "scripts": {
    "start": "node scripts/dev-serve.mjs",
    "build": "npm run build --workspace=frontend",
    "audio:registry": "node scripts/generate-audio-registry.mjs",
    "assets:fetch": "node scripts/fetch-assets.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
