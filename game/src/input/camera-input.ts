export interface CameraInput {
  cameraToggle: boolean;
  cameraCycle: number;
  /** Pulse — cycle chase spring profile (tactical / balanced / immersive). */
  cameraProfileCycle: boolean;
  cameraDistance: number;
  cameraOrbit: number;
  cameraDrop: boolean;
  lookAround: boolean;
}
