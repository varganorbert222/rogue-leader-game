/** Cruise speed when no throttle input; S brake holds here (Rogue Squadron style). */
export const MIN_FLIGHT_SPEED = 25;

export const DEFAULT_MAX_SPEED = 80;

export const DEFAULT_BOOST_MULTIPLIER = 1.6;

/** HUD reticle world-space distances along ship forward (meters). */
export const RETICLE_INNER_DISTANCE = 50;

export const RETICLE_OUTER_DISTANCE = 100;

/** Cosmetic yaw-bank on visual mesh (degrees). */
export const YAW_VISUAL_BANK_DEG = 15;
